# AirPower Cookie 模块

## 概述

AirPower Cookie模块是AirPower4J框架中的Cookie模块，提供统一的Cookie处理功能。

## 功能特性

- 统一Cookie处理

## 依赖关系

- `airpower-core`: 核心功能模块
- `airpower-exception`: 异常处理模块

## 使用场景

适用于需要 Cookie 支持的业务场景，提供标准化的Cookie 操作。

## 配置说明

模块遵循AirPower框架的统一配置规范，支持自动装配和配置继承。