# AirPower HTTP 模块

## 概述
AirPower HTTP模块是AirPower4J框架中的HTTP客户端模块，提供统一的HTTP请求处理功能。

## 功能特性
- 统一HTTP请求接口
- 支持同步异步请求
- 请求响应拦截处理
- 连接池管理

## 依赖关系
- `airpower-core`: 核心功能模块
- `airpower-exception`: 异常处理模块

## 使用场景
适用于需要发起HTTP请求的业务场景，提供标准化的HTTP客户端接口。

## 配置说明
模块遵循AirPower框架的统一配置规范，支持自动装配和配置继承。