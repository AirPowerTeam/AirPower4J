# AirPower4J 版本升级记录

## 任务目标

将 AirPower4J 根模块及子模块版本从 `6.3.0` 升级到 `6.3.1`。

## 完成内容

- 更新根 `pom.xml`：
    - `<version>6.3.0</version>` → `<version>6.3.1</version>`
    - `<airpower.version>6.3.0</airpower.version>` → `<airpower.version>6.3.1</airpower.version>`
- 更新以下子模块 `parent.version`：
    - `airpower-ai/pom.xml`
    - `airpower-api/pom.xml`
    - `airpower-cookie/pom.xml`
    - `airpower-curd/pom.xml`
    - `airpower-email/pom.xml`
    - `airpower-exception/pom.xml`
    - `airpower-mqtt/pom.xml`
    - `airpower-open/pom.xml`
    - `airpower-redis/pom.xml`
    - `airpower-websocket/pom.xml`

## 验证结果

- `grep_search` 确认 `**/pom.xml` 中已无 `6.3.0`。
- `grep_search` 确认 `**/pom.xml` 中共有 12 处 `6.3.1`。
- 执行 `zsh -ic 'cd /Users/hamm/Git/AirPower-Team/java/AirPower4J && mvn test -q'` 失败。

## 失败原因

Maven 无法解析依赖：

```text
Could not find artifact cn.hamm:airpower-core:jar:6.1.2 in alimaven (http://maven.aliyun.com/nexus/content/groups/public/)
```

该失败与本次 `6.3.0` → `6.3.1` 版本替换无直接关系。

## Git 状态

以下文件被修改：

```text
M airpower-ai/pom.xml
M airpower-api/pom.xml
M airpower-cookie/pom.xml
M airpower-curd/pom.xml
M airpower-email/pom.xml
M airpower-exception/pom.xml
M airpower-mqtt/pom.xml
M airpower-open/pom.xml
M airpower-redis/pom.xml
M airpower-websocket/pom.xml
M pom.xml
```
